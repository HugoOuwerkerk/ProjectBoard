# FastAPI environment configuration
# Copy this file to `.env` and adjust values for your deployment.

# Initial admin account (used only if the users table is empty on startup)
DEFAULT_ADMIN_USER=admin
DEFAULT_ADMIN_PASSWORD=change-me-please

# Cookie security
# Set to "true" in production so the session cookie is only sent over HTTPS.
SESSION_COOKIE_SECURE=false
